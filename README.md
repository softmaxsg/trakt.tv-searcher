# Sample application that allows to search movies on trakt.tv

Before compiling the project:

1. Make sure that Carthage is installed

2. Run the following command:

`
carthage update --platform ios
`
